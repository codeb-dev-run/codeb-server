name: CodeB CI/CD Pipeline

on:
  push:
    branches: [ main, develop, release/* ]
  pull_request:
    branches: [ main, develop ]
  workflow_dispatch:

env:
  NODE_VERSION: '18.x'
  REGISTRY: ghcr.io
  IMAGE_NAME: ${{ github.repository }}

jobs:
  # 1. Code Quality & Linting
  quality-check:
    name: Code Quality Check
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: ${{ env.NODE_VERSION }}
          cache: 'npm'
      
      - name: Install dependencies
        run: npm ci
      
      - name: Run ESLint
        id: lint
        continue-on-error: false
        run: npm run lint

      - name: Check code formatting
        id: format
        continue-on-error: false
        run: npm run format:check

      - name: Type checking
        id: typecheck
        continue-on-error: false
        run: npm run type-check

  # 2. Security Scanning
  security-scan:
    name: Security Vulnerability Scan
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: ${{ env.NODE_VERSION }}
          cache: 'npm'
      
      - name: Install dependencies
        run: npm ci
      
      - name: Run npm audit
        id: audit
        continue-on-error: false
        run: npm audit --audit-level=moderate

      - name: Run custom security scanner
        id: security-scan
        continue-on-error: false
        run: |
          if [ -f "scripts/security-scanner.js" ]; then
            node scripts/security-scanner.js
          else
            echo "Security scanner script not found, skipping..."
          fi
      
      - name: Upload security report
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: security-report
          path: .codeb-checkpoint/security-report-*.json

  # 3. Unit & Integration Tests
  test:
    name: Run Tests
    runs-on: ubuntu-latest
    services:
      postgres:
        image: postgres:14
        env:
          POSTGRES_PASSWORD: postgres
          POSTGRES_DB: codeb_test
        options: >-
          --health-cmd pg_isready
          --health-interval 10s
          --health-timeout 5s
          --health-retries 5
        ports:
          - 5432:5432
      
      redis:
        image: redis:7
        options: >-
          --health-cmd "redis-cli ping"
          --health-interval 10s
          --health-timeout 5s
          --health-retries 5
        ports:
          - 6379:6379
    
    steps:
      - uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: ${{ env.NODE_VERSION }}
          cache: 'npm'
      
      - name: Install dependencies
        run: npm ci
      
      - name: Run unit tests
        id: unit-tests
        continue-on-error: false
        run: npm test
        env:
          DATABASE_URL: postgresql://postgres:postgres@localhost:5432/codeb_test
          REDIS_URL: redis://localhost:6379

      - name: Run integration tests
        id: integration-tests
        continue-on-error: false
        run: |
          if npm run | grep -q "test:integration"; then
            npm run test:integration
          else
            echo "No integration tests configured"
          fi
        env:
          DATABASE_URL: postgresql://postgres:postgres@localhost:5432/codeb_test
          REDIS_URL: redis://localhost:6379

      - name: Generate coverage report
        id: coverage
        continue-on-error: false
        run: |
          if npm run | grep -q "test:coverage"; then
            npm run test:coverage
          else
            echo "No coverage script configured"
          fi
      
      - name: Upload coverage to Codecov
        uses: codecov/codecov-action@v4
        with:
          files: ./coverage/lcov.info
          fail_ci_if_error: false

  # 4. Load Testing
  load-test:
    name: Performance & Load Testing
    runs-on: ubuntu-latest
    needs: [quality-check, test]
    if: github.event_name == 'push' && github.ref == 'refs/heads/main'
    steps:
      - uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: ${{ env.NODE_VERSION }}
          cache: 'npm'
      
      - name: Install dependencies
        run: npm ci
      
      - name: Start server
        run: |
          npm start &
          sleep 10
        env:
          NODE_ENV: test
          PORT: 3000
      
      - name: Run load tests
        id: load-tests
        continue-on-error: false
        run: |
          if [ -f "scripts/load-testing.js" ]; then
            node scripts/load-testing.js
          else
            echo "Load testing script not found"
            exit 1
          fi
        env:
          TARGET_URL: http://localhost:3000
          CONCURRENT_USERS: 50
          TEST_DURATION: 30000
      
      - name: Upload load test report
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: load-test-report
          path: .codeb-checkpoint/load-test-report-*.json

  # 5. Build Docker Image
  build:
    name: Build and Push Docker Image
    runs-on: ubuntu-latest
    needs: [quality-check, security-scan, test]
    if: github.event_name == 'push'
    permissions:
      contents: read
      packages: write
    
    steps:
      - uses: actions/checkout@v4
      
      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3
      
      - name: Log in to Container Registry
        uses: docker/login-action@v3
        with:
          registry: ${{ env.REGISTRY }}
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}
      
      - name: Extract metadata
        id: meta
        uses: docker/metadata-action@v5
        with:
          images: ${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}
          tags: |
            type=ref,event=branch
            type=ref,event=pr
            type=semver,pattern={{version}}
            type=semver,pattern={{major}}.{{minor}}
            type=sha,prefix={{branch}}-
      
      - name: Build and push Docker image
        uses: docker/build-push-action@v6
        with:
          context: .
          push: true
          tags: ${{ steps.meta.outputs.tags }}
          labels: ${{ steps.meta.outputs.labels }}
          cache-from: type=gha
          cache-to: type=gha,mode=max
          platforms: linux/amd64,linux/arm64

  # 6. Deploy to Staging
  deploy-staging:
    name: Deploy to Staging
    runs-on: ubuntu-latest
    needs: [build]
    if: github.ref == 'refs/heads/develop'
    environment:
      name: staging
      url: https://staging.codeb.io
    
    steps:
      - uses: actions/checkout@v4
      
      - name: Deploy to staging server
        run: |
          echo "Deploying to staging environment..."
          # Add actual deployment commands here
          # ssh deploy@staging.server 'docker pull $IMAGE && docker restart codeb'
      
      - name: Run smoke tests
        run: |
          echo "Running smoke tests on staging..."
          # curl -f https://staging.codeb.io/health || exit 1

  # 7. Deploy to Production
  deploy-production:
    name: Deploy to Production
    runs-on: ubuntu-latest
    needs: [build, load-test]
    if: github.ref == 'refs/heads/main'
    environment:
      name: production
      url: https://codeb.io
    
    steps:
      - uses: actions/checkout@v4
      
      - name: Deploy to production server
        run: |
          echo "Deploying to production environment..."
          # Add actual deployment commands here
          # ssh deploy@prod.server 'docker pull $IMAGE && docker restart codeb'
      
      - name: Run smoke tests
        run: |
          echo "Running smoke tests on production..."
          # curl -f https://codeb.io/health || exit 1
      
      - name: Notify deployment
        if: always()
        run: |
          echo "Deployment status: ${{ job.status }}"
          # Add notification logic (Slack, Discord, email, etc.)

  # 8. E2E Tests
  e2e-tests:
    name: E2E Tests
    runs-on: ubuntu-latest
    needs: [deploy-staging]
    if: github.ref == 'refs/heads/develop'
    
    steps:
      - uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: ${{ env.NODE_VERSION }}
          cache: 'npm'
      
      - name: Install Playwright
        run: |
          npm ci
          npx playwright install --with-deps
      
      - name: Run E2E tests
        id: e2e-tests
        continue-on-error: false
        run: |
          if npm run | grep -q "test:e2e"; then
            npm run test:e2e
          else
            echo "No E2E tests configured"
          fi
        env:
          BASE_URL: https://staging.codeb.io
      
      - name: Upload E2E test results
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: e2e-test-results
          path: test-results/

  # 9. Generate Documentation
  generate-docs:
    name: Generate API Documentation
    runs-on: ubuntu-latest
    needs: [quality-check]
    if: github.ref == 'refs/heads/main'
    
    steps:
      - uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: ${{ env.NODE_VERSION }}
          cache: 'npm'
      
      - name: Install dependencies
        run: npm ci
      
      - name: Generate API docs
        id: docs
        continue-on-error: false
        run: |
          if npm run | grep -q "docs:generate"; then
            npm run docs:generate
          else
            echo "No docs:generate script configured"
          fi
      
      - name: Deploy docs to GitHub Pages
        if: success()
        uses: peaceiris/actions-gh-pages@v4
        with:
          github_token: ${{ secrets.GITHUB_TOKEN }}
          publish_dir: ./docs

  # 10. Performance Monitoring
  performance-monitor:
    name: Performance Monitoring
    runs-on: ubuntu-latest
    needs: [deploy-production]
    if: github.ref == 'refs/heads/main'
    
    steps:
      - uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: ${{ env.NODE_VERSION }}
          cache: 'npm'
      
      - name: Install dependencies
        run: npm ci
      
      - name: Run performance analysis
        id: perf-analysis
        continue-on-error: false
        run: |
          if [ -f "scripts/performance-analyzer.js" ]; then
            node scripts/performance-analyzer.js
          else
            echo "Performance analyzer script not found"
            exit 1
          fi
      
      - name: Check performance metrics
        run: |
          echo "Checking production performance metrics..."
          # Add performance monitoring logic
      
      - name: Alert on performance degradation
        if: failure()
        run: |
          echo "Performance degradation detected!"
          # Add alerting logic