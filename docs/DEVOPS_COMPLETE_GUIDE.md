# CodeB DevOps 완전 가이드

> **버전**: 1.0.0
> **작성일**: 2025-01-10
> **대상**: 10인 개발팀 + 스타트업/사이드 프로젝트
> **철학**: 쿠버네티스 없이 엔터프라이즈급 자동화 달성

---

## 목차

1. [DevOps 개요](#1-devops-개요)
2. [현재 구축 상태 vs 이상적 구조](#2-현재-구축-상태-vs-이상적-구조)
3. [기술 스택 상세](#3-기술-스택-상세)
4. [레이어별 아키텍처](#4-레이어별-아키텍처)
5. [CI/CD 파이프라인](#5-cicd-파이프라인)
6. [IaC (Infrastructure as Code)](#6-iac-infrastructure-as-code)
7. [런타임 운영 구조](#7-런타임-운영-구조)
8. [AI 기반 자동화](#8-ai-기반-자동화)
9. [모니터링 & 관측성](#9-모니터링--관측성)
10. [로드맵](#10-로드맵)

---

## 1. DevOps 개요

### 1.1 DevOps란?

**Development(개발) + Operations(운영)**을 자동화·통합하여 빠르고 안정적인 배포를 실현하는 방식(문화 + 시스템)

```
┌─────────────────────────────────────────────────────────────┐
│                      DevOps 사이클                          │
├─────────────────────────────────────────────────────────────┤
│                                                             │
│    Plan → Code → Build → Test → Release → Deploy → Operate │
│      ↑                                               │      │
│      └───────────────── Monitor ←────────────────────┘      │
│                                                             │
└─────────────────────────────────────────────────────────────┘
```

### 1.2 핵심 목표

| 목표 | 설명 | 우리의 구현 |
|------|------|------------|
| **자동화** | 수동 작업 최소화 | GitHub Actions + MCP |
| **반복 가능** | 동일 결과 보장 | Podman + Compose |
| **오류 최소화** | 사전 검증 | Self-Healing CI/CD |
| **빠른 배포** | 짧은 리드 타임 | Rolling 배포 |
| **운영 안정성** | 무중단 서비스 | 헬스체크 + 자동 롤백 |

### 1.3 우리의 철학

```
🎯 "쿠버네티스 없이도 엔터프라이즈급 자동화"

- 10인 팀에 K8s는 오버엔지니어링
- Podman + systemd로 충분한 안정성
- 복잡도 vs 가치의 균형
```

---

## 2. 현재 구축 상태 vs 이상적 구조

### 2.1 비교표

| 영역 | 이상적 구조 | 현재 상태 | 갭 분석 |
|------|------------|----------|--------|
| **소스 저장소** | GitHub | ✅ GitHub | 완료 |
| **CI/CD** | GitHub Actions | ✅ GitHub Actions | 완료 |
| **컨테이너 엔진** | Podman | ✅ Podman 3.4.4 | 완료 |
| **오케스트레이션** | Quadlet + systemd | ✅ 설정 파일 준비됨 | Podman 4.4+ 업그레이드 필요 |
| **IaC** | Terraform | ✅ 설정 파일 준비됨 | 적용 대기 |
| **서버 설정** | Ansible | ✅ Playbook 준비됨 | 적용 대기 |
| **모니터링** | Prometheus/Grafana | ✅ MCP 스크립트 | 경량 유지 (의도적) |
| **AI 자동화** | Copilot/LLM | ✅ Claude API | 완료 |
| **자동 백업** | Cron + Script | ✅ 스크립트 준비됨 | 서버 적용 대기 |

### 2.2 현재 아키텍처

```
┌─────────────────────────────────────────────────────────────┐
│                     현재 CodeB 구조                          │
├─────────────────────────────────────────────────────────────┤
│                                                             │
│  Developer                                                  │
│      │                                                      │
│      ▼                                                      │
│  ┌─────────┐    ┌──────────────┐    ┌─────────────┐        │
│  │ GitHub  │ → │ GitHub       │ → │ ghcr.io     │        │
│  │ (코드)   │    │ Actions (CI) │    │ (이미지)     │        │
│  └─────────┘    └──────────────┘    └─────────────┘        │
│                        │                    │               │
│                        ▼                    ▼               │
│                 ┌──────────────────────────────┐            │
│                 │      Vultr VPS (141.x.x.x)   │            │
│                 │  ┌──────────────────────┐    │            │
│                 │  │ Podman Containers    │    │            │
│                 │  │  ├── App             │    │            │
│                 │  │  ├── PostgreSQL      │    │            │
│                 │  │  └── Redis           │    │            │
│                 │  └──────────────────────┘    │            │
│                 │  ┌──────────────────────┐    │            │
│                 │  │ Caddy (HTTPS)        │    │            │
│                 │  │ PowerDNS             │    │            │
│                 │  │ PM2 (Node.js)        │    │            │
│                 │  └──────────────────────┘    │            │
│                 └──────────────────────────────┘            │
│                                                             │
└─────────────────────────────────────────────────────────────┘
```

### 2.3 목표 아키텍처 (향후)

```
┌─────────────────────────────────────────────────────────────┐
│                    목표 DevOps 구조                          │
├─────────────────────────────────────────────────────────────┤
│                                                             │
│  ┌─────────┐                                                │
│  │ GitHub  │ ─── PR/Push ───┐                              │
│  └─────────┘                │                              │
│       │                     ▼                              │
│       │            ┌──────────────────┐                    │
│       │            │ GitHub Actions   │                    │
│       │            │  ├── Build       │                    │
│       │            │  ├── Test        │                    │
│       │            │  ├── AI Fix      │ ← Self-Healing    │
│       │            │  └── Push Image  │                    │
│       │            └──────────────────┘                    │
│       │                     │                              │
│       │                     ▼                              │
│       │            ┌──────────────────┐                    │
│       │            │ Terraform (IaC)  │ ← 인프라 코드화    │
│       │            └──────────────────┘                    │
│       │                     │                              │
│       │                     ▼                              │
│       │            ┌──────────────────┐                    │
│       │            │ Ansible          │ ← 서버 설정 자동화 │
│       │            └──────────────────┘                    │
│       │                     │                              │
│       ▼                     ▼                              │
│  ┌─────────────────────────────────────────────────┐       │
│  │              Vultr VPS (Production)             │       │
│  │  ┌─────────────────────────────────────────┐    │       │
│  │  │ Quadlet + systemd (런타임 관리)          │    │       │
│  │  │  ├── app.container (자동 재시작)         │    │       │
│  │  │  ├── postgres.container                  │    │       │
│  │  │  └── redis.container                     │    │       │
│  │  └─────────────────────────────────────────┘    │       │
│  │  ┌─────────────────────────────────────────┐    │       │
│  │  │ 모니터링 (Prometheus/Grafana 또는 경량)  │    │       │
│  │  └─────────────────────────────────────────┘    │       │
│  └─────────────────────────────────────────────────┘       │
│                                                             │
└─────────────────────────────────────────────────────────────┘
```

---

## 3. 기술 스택 상세

### 3.1 전체 스택 (한글 발음 포함)

| 영역 | 기술 | 한글 발음 | 상태 | 용도 |
|------|------|----------|------|------|
| 소스 저장소 | GitHub | 깃허브 | ✅ | 코드 관리 |
| CI/CD | GitHub Actions | 깃허브 액션즈 | ✅ | 빌드/배포 자동화 |
| 컨테이너 엔진 | Podman | 포드맨 | ✅ | Docker 대체 |
| 컨테이너 오케스트레이션 | Quadlet | 쿼들릿 | ⚠️ | systemd 통합 |
| 시스템 서비스 | systemd | 시스템디 | ⚠️ | 프로세스 관리 |
| IaC | Terraform | 테라폼 | ❌ | 인프라 코드화 |
| 서버 설정 | Ansible | 앤서블 | ❌ | 설정 자동화 |
| 웹서버/프록시 | Caddy | 캐디 | ✅ | HTTPS, 리버스 프록시 |
| DNS | PowerDNS | 파워DNS | ✅ | 도메인 관리 |
| 데이터베이스 | PostgreSQL | 포스트그레스큐엘 | ✅ | 메인 DB |
| 캐시 | Redis | 레디스 | ✅ | 세션/캐시 |
| AI 자동화 | Claude API | 클로드 | ✅ | Self-Healing |
| 보안 스캔 | Trivy | 트리비 | ✅ | 취약점 분석 |
| 시크릿 스캔 | gitleaks | 깃릭스 | ✅ | 비밀키 탐지 |
| SBOM | syft | 시프트 | ✅ | 의존성 목록 |
| CLI | gh | 지에이치 | ✅ | GitHub CLI |

### 3.2 왜 이 스택인가?

#### Podman vs Docker

| 항목 | Docker | Podman | 선택 |
|------|--------|--------|------|
| Daemon | 필요 (root) | 불필요 (rootless) | **Podman** |
| 보안 | root 권한 필요 | 일반 사용자 실행 | **Podman** |
| 호환성 | 업계 표준 | Docker CLI 호환 | 동등 |
| 설치 상태 | 미설치 | 이미 설치됨 | **Podman** |

#### Quadlet + systemd vs Kubernetes

| 항목 | Kubernetes | Quadlet + systemd | 선택 |
|------|------------|-------------------|------|
| 복잡도 | 매우 높음 | 낮음 | **Quadlet** |
| 학습 곡선 | 가파름 | 완만함 | **Quadlet** |
| 리소스 | 최소 3노드 권장 | 단일 서버 가능 | **Quadlet** |
| 자동 재시작 | Pod 레벨 | 컨테이너 레벨 | 동등 |
| 적합한 규모 | 50명+ | 1-30명 | **Quadlet** |

#### GitHub Actions vs Self-Hosted Runner

| 항목 | GitHub Actions | Self-Hosted | 선택 |
|------|---------------|-------------|------|
| 비용 | 무료 2000분/월 | 서버 리소스 | **GitHub Actions** |
| 관리 | 불필요 | 업데이트 필요 | **GitHub Actions** |
| 서버 영향 | 없음 | 빌드 시 영향 | **GitHub Actions** |
| 캐시 속도 | 느림 | 빠름 | Self-Hosted |

---

## 4. 레이어별 아키텍처

### 4.1 7-Layer DevOps 구조

```
┌─────────────────────────────────────────────────────────────┐
│ Layer 7: 모니터링 & 관측                                     │
│ (Prometheus, Grafana, 또는 MCP 스크립트)                     │
├─────────────────────────────────────────────────────────────┤
│ Layer 6: AI 자동화                                          │
│ (Claude API - Self-Healing, 에러 분석)                      │
├─────────────────────────────────────────────────────────────┤
│ Layer 5: 런타임 관리                                        │
│ (Quadlet + systemd - 자동 재시작, 헬스체크)                  │
├─────────────────────────────────────────────────────────────┤
│ Layer 4: 컨테이너 실행                                      │
│ (Podman - 앱, DB, 캐시)                                     │
├─────────────────────────────────────────────────────────────┤
│ Layer 3: 배포 자동화                                        │
│ (GitHub Actions → SSH → Podman pull/run)                   │
├─────────────────────────────────────────────────────────────┤
│ Layer 2: 인프라 프로비저닝                                   │
│ (Terraform - Vultr VPS 생성) [향후 추가]                    │
├─────────────────────────────────────────────────────────────┤
│ Layer 1: 코드 관리                                          │
│ (GitHub - 소스, PR, Issues)                                 │
└─────────────────────────────────────────────────────────────┘
```

### 4.2 레이어별 상세

| 레이어 | 역할 | 현재 도구 | 향후 계획 |
|--------|------|----------|----------|
| L1 | 코드 관리 | GitHub | 유지 |
| L2 | 인프라 | 수동 Vultr 콘솔 | Terraform |
| L3 | 배포 | GitHub Actions + SSH | 유지 |
| L4 | 컨테이너 | Podman | 유지 |
| L5 | 런타임 | 수동 podman run | Quadlet |
| L6 | AI | Claude API | 유지 |
| L7 | 모니터링 | MCP 스크립트 | 선택적 Prometheus |

---

## 5. CI/CD 파이프라인

### 5.1 파이프라인 흐름

```
┌─────────────────────────────────────────────────────────────┐
│                    CI/CD 파이프라인                          │
├─────────────────────────────────────────────────────────────┤
│                                                             │
│  1. 코드 Push/PR                                            │
│       │                                                     │
│       ▼                                                     │
│  2. GitHub Actions 트리거                                   │
│       │                                                     │
│       ├── 2.1 TypeCheck (tsc)                              │
│       ├── 2.2 Lint (eslint)                                │
│       ├── 2.3 Build (next build)                           │
│       └── 2.4 Test (jest)                                  │
│       │                                                     │
│       ├── 성공 ──────────────────────┐                     │
│       │                              │                     │
│       └── 실패 ──┐                   │                     │
│                  │                   │                     │
│  3. Self-Healing │                   │                     │
│       │          │                   │                     │
│       ├── 3.1 에러 로그 수집          │                     │
│       ├── 3.2 Claude API 호출        │                     │
│       ├── 3.3 No-Deletion 검증       │                     │
│       └── 3.4 자동 커밋 & 재시도      │                     │
│              (최대 5회)              │                     │
│                                      │                     │
│  4. Docker 빌드 ◀────────────────────┘                     │
│       │                                                     │
│       ▼                                                     │
│  5. ghcr.io 푸시                                            │
│       │                                                     │
│       ▼                                                     │
│  6. SSH 배포                                                │
│       │                                                     │
│       ├── 6.1 podman pull                                  │
│       ├── 6.2 podman stop (기존)                           │
│       ├── 6.3 podman run (신규)                            │
│       └── 6.4 헬스체크                                     │
│       │                                                     │
│       ├── 성공 → 완료                                      │
│       └── 실패 → 자동 롤백                                 │
│                                                             │
└─────────────────────────────────────────────────────────────┘
```

### 5.2 Self-Healing 상세

```yaml
# .github/workflows/self-healing-ci.yml 핵심 부분

jobs:
  build:
    runs-on: ubuntu-latest  # Self-Hosted 아님!
    steps:
      - run: npm run typecheck
      - run: npm run lint
      - run: npm run build
      - run: npm run test

  auto-fix:
    needs: build
    if: failure()  # 빌드 실패 시만 실행
    steps:
      # 1. 에러 로그 파싱
      - name: Parse Error Logs
        run: |
          # TypeScript, ESLint, Jest 에러 분류

      # 2. Claude API 호출
      - name: Request AI Fix
        env:
          ANTHROPIC_API_KEY: ${{ secrets.ANTHROPIC_API_KEY }}
        run: |
          # No-Deletion 원칙이 포함된 프롬프트

      # 3. 검증 (삭제 > 추가 면 거부)
      - name: Validate Fix
        run: |
          if [ "$DELETIONS" -gt "$ADDITIONS" ]; then
            exit 1
          fi

      # 4. 재빌드
      - name: Commit & Push
        run: git push
```

### 5.3 No-Deletion 원칙

**절대 금지 패턴**:
```typescript
const FORBIDDEN = [
  '@ts-ignore',      // 타입 에러 무시
  '@ts-nocheck',     // 파일 전체 타입 무시
  'eslint-disable',  // 린트 무시
  '.skip(',          // 테스트 스킵
  'as any',          // any 타입 캐스팅
  ': any',           // any 타입 선언
];
```

**검증 로직**:
```bash
# 삭제가 추가보다 많으면 거부
DELETIONS=$(git diff --numstat | awk '{sum+=$2} END{print sum}')
ADDITIONS=$(git diff --numstat | awk '{sum+=$1} END{print sum}')

if [ "$DELETIONS" -gt "$ADDITIONS" ]; then
  echo "❌ 코드 삭제로 문제 해결 시도 감지 - 거부됨"
  exit 1
fi
```

---

## 6. IaC (Infrastructure as Code)

### 6.1 현재 상태

```
현재: 수동으로 Vultr 콘솔에서 서버 생성
문제: 재현 불가능, 문서화 어려움, 실수 가능
```

### 6.2 목표: Terraform 도입

```hcl
# terraform/main.tf (향후 추가 예정)

provider "vultr" {
  api_key = var.vultr_api_key
}

resource "vultr_instance" "codeb_server" {
  plan             = "vc2-2c-4gb"  # 2 vCPU, 4GB RAM
  region           = "icn"         # 서울
  os_id            = 1743          # Ubuntu 22.04
  label            = "codeb-production"
  hostname         = "codeb-prod"
  enable_ipv6      = true
  backups          = "enabled"

  ssh_key_ids = [vultr_ssh_key.deploy.id]
}

resource "vultr_dns_domain" "codeb" {
  domain = "codeb.dev"
}

resource "vultr_dns_record" "wildcard" {
  domain = vultr_dns_domain.codeb.id
  name   = "*"
  type   = "A"
  data   = vultr_instance.codeb_server.main_ip
  ttl    = 300
}
```

### 6.3 Terraform 도입 이점

| 항목 | 수동 | Terraform |
|------|------|-----------|
| 재현성 | ❌ | ✅ `terraform apply` |
| 버전 관리 | ❌ | ✅ Git으로 변경 추적 |
| 문서화 | ❌ | ✅ 코드가 문서 |
| 롤백 | ❌ | ✅ `terraform destroy` |
| 멀티 환경 | 어려움 | ✅ workspace |

---

## 7. 런타임 운영 구조

### 7.1 현재: 수동 Podman

```bash
# 현재 방식 (수동)
podman run -d --name myapp -p 3000:3000 ghcr.io/org/app:latest

# 문제점:
# - 서버 재부팅 시 수동 재시작 필요
# - 크래시 시 자동 복구 없음
# - 의존성 순서 관리 어려움
```

### 7.2 목표: Quadlet + systemd

```ini
# /etc/containers/systemd/myapp.container (향후 추가)

[Unit]
Description=MyApp Container
After=network-online.target postgres.service redis.service
Wants=network-online.target

[Container]
Image=ghcr.io/org/app:latest
PublishPort=3000:3000
Environment=NODE_ENV=production
Environment=DATABASE_URL=postgresql://...
Volume=/opt/codeb/data:/app/data:Z
HealthCmd=/usr/bin/curl -f http://localhost:3000/health
HealthInterval=30s
HealthRetries=3

[Service]
Restart=always
RestartSec=5
TimeoutStartSec=180

[Install]
WantedBy=multi-user.target
```

```bash
# Quadlet 사용 시
systemctl --user daemon-reload
systemctl --user enable --now myapp

# 자동으로:
# - 서버 부팅 시 시작
# - 크래시 시 재시작
# - 의존성 순서 보장
# - 로그 journalctl로 통합
```

### 7.3 Quadlet 도입 이점

| 항목 | 수동 Podman | Quadlet + systemd |
|------|------------|-------------------|
| 자동 시작 | ❌ | ✅ |
| 크래시 복구 | ❌ | ✅ `Restart=always` |
| 의존성 | 수동 | ✅ `After=` |
| 로그 통합 | ❌ | ✅ `journalctl` |
| 헬스체크 | 수동 | ✅ `HealthCmd=` |

---

## 8. AI 기반 자동화

### 8.1 현재 구현

```
┌─────────────────────────────────────────────────────────────┐
│                    AI 자동화 흐름                            │
├─────────────────────────────────────────────────────────────┤
│                                                             │
│  빌드 실패                                                   │
│      │                                                      │
│      ▼                                                      │
│  ┌──────────────────┐                                       │
│  │ 에러 로그 수집    │                                       │
│  │ - TypeScript     │                                       │
│  │ - ESLint         │                                       │
│  │ - Jest           │                                       │
│  └──────────────────┘                                       │
│      │                                                      │
│      ▼                                                      │
│  ┌──────────────────┐                                       │
│  │ Claude API 호출  │                                       │
│  │                  │                                       │
│  │ 프롬프트:        │                                       │
│  │ - 에러 분석      │                                       │
│  │ - No-Deletion    │                                       │
│  │ - 허용된 수정만  │                                       │
│  └──────────────────┘                                       │
│      │                                                      │
│      ▼                                                      │
│  ┌──────────────────┐                                       │
│  │ 검증             │                                       │
│  │ - 삭제 비율      │                                       │
│  │ - 금지 패턴      │                                       │
│  └──────────────────┘                                       │
│      │                                                      │
│      ├── 통과 → 커밋 & 재빌드                               │
│      └── 실패 → 알림 (수동 개입)                            │
│                                                             │
└─────────────────────────────────────────────────────────────┘
```

### 8.2 MCP 도구

| 도구 | 용도 | 호출 예시 |
|------|------|----------|
| `get_build_errors` | 빌드 에러 조회 | 워크플로우 실패 시 |
| `validate_fix` | 수정 검증 | AI 수정 후 |
| `auto_fix_build_loop` | 자동 수정 루프 | 최대 5회 |
| `generate_fix_prompt` | AI 프롬프트 생성 | 에러별 맞춤 |

### 8.3 AI 확장 계획

| 기능 | 현재 | 계획 |
|------|------|------|
| 빌드 에러 수정 | ✅ | 유지 |
| PR 코드 리뷰 | ❌ | 추가 예정 |
| 테스트 생성 | ❌ | 추가 예정 |
| 문서 자동 생성 | ❌ | 추가 예정 |

---

## 9. 모니터링 & 관측성

### 9.1 현재: MCP 스크립트 기반

```typescript
// MCP 도구로 구현된 모니터링

// 디스크 모니터링
await monitorDisk();
// 결과: 80% 경고, 90% 위험 알림

// SSL 모니터링
await monitorSSL();
// 결과: 7일 위험, 14일 경고

// 컨테이너 헬스체크
await checkContainerHealth();
// 결과: 비정상 컨테이너 탐지

// 백업 상태
await checkBackupStatus();
// 결과: 24시간 이상 미백업 경고

// 종합 헬스체크
await fullHealthCheck();
// 결과: 전체 상태 요약
```

### 9.2 선택: 경량 vs Prometheus/Grafana

| 항목 | 경량 (MCP) | Prometheus/Grafana |
|------|-----------|-------------------|
| 리소스 | 최소 | 500MB+ RAM |
| 설정 | 간단 | 복잡 |
| 시각화 | CLI | 대시보드 |
| 알림 | Slack/Email | 다양한 채널 |
| **10인 팀 적합성** | ✅ | ⚠️ 과도함 |

### 9.3 모니터링 임계값

| 항목 | 경고 (Warning) | 위험 (Critical) |
|------|---------------|-----------------|
| 디스크 | 80% | 90% |
| SSL 만료 | 14일 | 7일 |
| 컨테이너 재시작 | 3회 | 5회 |
| 백업 | 24시간 | 48시간 |
| 메모리 | 80% | 90% |

---

## 10. 로드맵

### 10.1 단계별 계획

```
┌─────────────────────────────────────────────────────────────┐
│                       로드맵                                 │
├─────────────────────────────────────────────────────────────┤
│                                                             │
│  Phase 1: 기반 구축 (✅ 완료)                               │
│  ├── ✅ GitHub Actions CI/CD                               │
│  ├── ✅ Self-Healing (Claude API)                          │
│  ├── ✅ Podman 컨테이너                                     │
│  ├── ✅ MCP 모니터링 스크립트                               │
│  └── ✅ 문서화                                              │
│                                                             │
│  Phase 2: 런타임 안정화 (✅ 파일 준비 완료)                  │
│  ├── ✅ Quadlet 설정 파일 작성                              │
│  ├── ✅ 자동 백업 스크립트                                  │
│  └── ⏳ 서버 적용 대기 (Podman 4.4+ 필요)                  │
│                                                             │
│  Phase 3: IaC 도입 (✅ 파일 준비 완료)                      │
│  ├── ✅ Terraform 설정 (Vultr VPS, DNS, 방화벽)            │
│  ├── ✅ Ansible Playbook (setup, deploy, backup)          │
│  └── ⏳ 적용 대기                                          │
│                                                             │
│  Phase 4: 확장 (팀 성장 시 - 미착수)                        │
│  ├── ❌ Prometheus/Grafana (20명+)                         │
│  ├── ❌ Blue-Green 배포 (30명+)                            │
│  └── ❌ Kubernetes 검토 (50명+)                            │
│                                                             │
└─────────────────────────────────────────────────────────────┘
```

### 10.2 구체적 TODO

| 우선순위 | 작업 | 상태 | 파일 위치 |
|---------|------|------|----------|
| P0 | Quadlet 설정 파일 작성 | ✅ 완료 | `infrastructure/quadlet/*.container` |
| P0 | systemd 서비스 등록 | ⏳ 서버 적용 대기 | `infrastructure/quadlet/install-quadlet.sh` |
| P1 | 자동 백업 cron 설정 | ✅ 스크립트 완료 | `infrastructure/scripts/setup-backup-cron.sh` |
| P1 | SSL 자동 갱신 확인 | ✅ MCP 도구 완료 | `mcp monitor_ssl` |
| P2 | Terraform 기본 설정 | ✅ 완료 | `infrastructure/terraform/*.tf` |
| P2 | Ansible playbook | ✅ 완료 | `infrastructure/ansible/playbooks/*.yml` |
| P3 | PR 자동 리뷰 AI | ❌ 미착수 | 향후 추가 예정 |

### 10.3 팀 성장 시 고려사항

| 팀 규모 | 추가 고려 |
|--------|----------|
| 10명 → 20명 | Prometheus 재검토 |
| 20명 → 30명 | Blue-Green 배포 |
| 30명 → 50명 | 전담 DevOps 인력 |
| 50명+ | Kubernetes 전환 검토 |

---

## 부록 A: 구현된 인프라 파일 목록

> **위치**: `infrastructure/` 디렉토리

### A.1 Quadlet (Podman + systemd)

```
infrastructure/quadlet/
├── app.container          # 앱 컨테이너 정의 (포트 3000)
├── postgres.container     # PostgreSQL 15 컨테이너
├── redis.container        # Redis 7 컨테이너
├── network.network        # 컨테이너 네트워크 (10.89.0.0/24)
├── config/
│   ├── app.env.example    # 앱 환경변수 템플릿
│   └── postgres.env.example # DB 환경변수 템플릿
└── install-quadlet.sh     # 설치 스크립트
```

**사용법**:
```bash
# 서버에서 실행
cd infrastructure/quadlet
bash install-quadlet.sh

# 서비스 시작
systemctl start codeb-postgres codeb-redis codeb-app

# 자동 시작 등록
systemctl enable codeb-postgres codeb-redis codeb-app
```

**주의**: Quadlet은 Podman 4.4+ 필요. 현재 서버 3.4.4 → 업그레이드 또는 수동 systemd 사용

### A.2 Terraform (IaC)

```
infrastructure/terraform/
├── main.tf                # VPS, DNS, 방화벽 리소스
├── variables.tf           # 변수 정의
├── terraform.tfvars.example # 변수 값 예시
└── scripts/
    └── user-data.sh       # 서버 초기화 스크립트
```

**사용법**:
```bash
cd infrastructure/terraform

# 설정
cp terraform.tfvars.example terraform.tfvars
# terraform.tfvars 편집 (API 키, SSH 키 등)

# 적용
terraform init
terraform plan
terraform apply
```

**생성되는 리소스**:
- Vultr VPS (2 vCPU, 4GB RAM)
- SSH 키 등록
- 방화벽 그룹 (22, 80, 443 포트)
- DNS 레코드 (선택적)

### A.3 Ansible Playbooks

```
infrastructure/ansible/
├── inventory.yml              # 서버 목록
└── playbooks/
    ├── setup-server.yml       # 초기 서버 설정
    ├── deploy-app.yml         # 앱 배포
    └── backup.yml             # 백업
```

**사용법**:
```bash
cd infrastructure/ansible

# 서버 초기 설정
ansible-playbook -i inventory.yml playbooks/setup-server.yml

# 앱 배포
ansible-playbook -i inventory.yml playbooks/deploy-app.yml -e "image_tag=v1.0.0"

# 백업
ansible-playbook -i inventory.yml playbooks/backup.yml
```

**setup-server.yml이 설치하는 것**:
- Podman, Node.js 20, PM2, Caddy, GitHub CLI
- 디렉토리 구조 (/opt/codeb/*)
- UFW 방화벽 (22, 80, 443)
- Swap 2GB
- 타임존 (Asia/Seoul)
- SSH 보안 설정
- 자동 보안 업데이트

### A.4 자동 백업 스크립트

```
infrastructure/scripts/
└── setup-backup-cron.sh   # Cron 자동 설정
```

**사용법**:
```bash
# 서버에서 실행
bash infrastructure/scripts/setup-backup-cron.sh
```

**설정되는 Cron**:
- 매일 새벽 2시: PostgreSQL, Redis, 설정 파일 백업
- 매 6시간: 디스크 용량 체크
- 7일 이상 된 백업 자동 삭제

---

## 부록 B: 용어 정리 (한글 발음)

| 영어 | 한글 발음 | 설명 |
|------|----------|------|
| GitHub | 깃허브 | 코드 저장소 |
| GitHub Actions | 깃허브 액션즈 | CI/CD 서비스 |
| Podman | 포드맨 | 컨테이너 엔진 |
| Quadlet | 쿼들릿 | systemd 통합 도구 |
| systemd | 시스템디 | Linux 서비스 관리자 |
| Terraform | 테라폼 | IaC 도구 |
| Ansible | 앤서블 | 서버 설정 자동화 |
| Prometheus | 프로메테우스 | 메트릭 수집 |
| Grafana | 그라파나 | 시각화 대시보드 |
| Caddy | 캐디 | 웹서버/리버스 프록시 |
| Redis | 레디스 | 인메모리 캐시 |
| PostgreSQL | 포스트그레스큐엘 | 관계형 DB |
| Vultr | 벌처 | VPS 제공업체 |
| ghcr.io | 지에이치씨알 | GitHub 컨테이너 레지스트리 |
| Trivy | 트리비 | 보안 스캐너 |

## 부록 B: 명령어 빠른 참조

```bash
# === GitHub CLI ===
gh run list                    # 워크플로우 실행 목록
gh run view <id> --log        # 실행 로그 보기
gh secret set NAME            # 시크릿 설정

# === Podman ===
podman ps -a                  # 모든 컨테이너
podman logs <name>            # 컨테이너 로그
podman pull ghcr.io/...       # 이미지 다운로드

# === systemd (Quadlet 적용 후) ===
systemctl --user status myapp # 서비스 상태
journalctl --user -u myapp    # 서비스 로그

# === MCP 도구 ===
mcp full_health_check         # 종합 헬스체크
mcp monitor_disk              # 디스크 확인
mcp monitor_ssl               # SSL 확인
mcp rollback                  # 롤백
```

---

**문서 끝**

> 이 문서는 CodeB 팀의 DevOps 구조를 정의합니다.
> 질문이나 제안은 팀 채널에 공유해주세요.
